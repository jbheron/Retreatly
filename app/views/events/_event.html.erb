<tr id ="first-tr-in-loop">
	<td><%= link_to event.title, event_path(event) %></td>
	<td><%= link_to "#{event.user.first_name} #{event.user.last_name}", user_path(event.user) %></td>
	<td><%= event.start_date.to_date %></td>
	<td><%= event.end_date.to_date %></td>
	<td><%= "#{event.venues[0].city}, #{event.venues[0].country}" %></td>
</tr>